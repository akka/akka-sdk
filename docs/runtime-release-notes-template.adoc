// Template for a per-release Akka Runtime release notes page.
//
// Copy to docs/src/modules/reference/pages/release-notes/runtime/<version>.adoc, fill it in, and
// add a row to release-notes/runtime/index.adoc. Keep the section order below. Omit a section when
// it has no entries; do not write "None".
//
// Source material: `git log v<previous>..v<version>` in the akka-runtime repository, plus the pull
// request bodies for anything whose user-visible effect is not obvious from the commit subject.
//
// Rules:
// - Describe the effect on a service, not the code change. "The configured `response-timeout` is
//   applied for OpenAI-based model providers", not "fix ChatModelFactory timeout handling".
// - Prefix each entry with the area it affects: Views, Agents, Workflows, Entity deletion, Eventing.
// - Leave out changes with no user-visible effect: build changes, formatting, internal refactoring,
//   and back office endpoints used only by Akka Platform operations tooling.
// - A runtime change that the Akka SDK does not expose yet goes in its own section, so the page
//   stays accurate about what a service can actually use.

= Akka Runtime <version>
:page-summary: <one sentence naming the main changes, not why they are good>

include::ROOT:partial$include.adoc[]

Released <YYYY-MM-DD>. Included in Akka SDK <version>. The previous runtime release is <version>.

== Changes to existing behavior

// Anything that changes what an already deployed service does without a code change: log severity,
// defaults, timeouts, retry behavior, which callbacks are invoked. This section is the reason the
// page exists, so it comes first.

Review these before you deploy a service on this runtime version.

* ...

== New features

// Capabilities a service can use through the Akka SDK release named above.

* ...

== Bug fixes

* ...

== Runtime capabilities not yet exposed by the Akka SDK

// Runtime changes with no SDK API in the paired SDK release. Name the SDK type that would carry it
// so the gap is greppable when the SDK catches up.

These changes are in the runtime but have no Akka SDK API in <version>.

* ...

== Security updates

// Dependency updates that address reported vulnerabilities. Give the component, the new version,
// and the severity counts. Note deliberately held-back dependencies and why, because customers run
// scanners against the runtime image and ask about them.

* ...

== Dependency updates

// Every dependency version change, security ones included. Flat list, no descriptions.

* ...

== Compatibility

// Rolling update support, configuration changes, and the minimum Akka SDK version. Confirm the
// rolling update statement with the runtime team for each release rather than copying it forward.

* ...
