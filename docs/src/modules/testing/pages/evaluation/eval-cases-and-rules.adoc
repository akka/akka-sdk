= Eval cases and rules
:page-summary: An eval case is one test case with a stable id. Every attempt against one case is gathered into an EvalCaseResults record, and the report reads verdicts case by case rather than attempt by attempt.
:page-when-to-use: Use when a case needs a stable identity across runs, when a case needs the conversation seeded before the graded turn, or when reading how a repeated case reaches one verdict.
:page-related: xref:testing:evaluation/evaluators.adoc[], xref:testing:evaluation/campaigns.adoc[], xref:testing:evaluation/reports.adoc[]
:page-banner: evaluations

include::ROOT:partial$include.adoc[]

An eval case is one test case the target reads and an evaluator settles.
`EvalCaseResults` is one eval case together with every attempt against it.

Running an eval case three times produces three attempts and one `EvalCaseResults`.
The report reads results case by case.
A case that passes twice and fails once is one varied verdict rather than a mixture of passes and failures.

== The EvalCase record

`EvalCase` is a Java record. Build the list in the campaign class.
evalkit ships no reader for an on-disk format, so where a project keeps its cases is its own choice.

[source,java,indent=0]
----
new EvalCase(
    "refund-timing",                           // <1>
    Optional.of("GenUC-16a"),                  // <2>
    Optional.empty(),                          // <3>
    List.of("30 days"),                        // <4>
    new EvalSetup.None(),                      // <5>
    "when do I get my refund?",                // <6>
    "the reply states a 30-day refund window"); // <7>
----
<1> The stable identifier the report references.
<2> The specification node the answer should come from, when the target tracks one.
Naming it lets the case be settled by comparison instead of by a model, avoiding a paid model call for a case with one right answer.
<3> A `MetricRef` when this case should be settled by a named metric.
<4> Wording the reply must contain.
Most cases require none, and a shorter constructor omits this.
<5> What has to happen before the graded turn.
See <<_eval_setups>>.
<6> The turn that gets scored.
<7> What a correct answer does, in a sentence.
This is what a judge reads the reply against.

Which evaluator settles an eval case follows from what it declares.
`EvaluatorRouter.byExpectation` sends a named spec node to comparison, required phrases to
`ContainsAll`, a named metric to that metric, and anything else to the judge.

[#_eval_setups]
== Eval setups

`EvalSetup` is a sealed interface with four cases.

`None` +
The graded turn is the first thing said.

`Replay(List<String> userTurns)` +
The listed turns are said first, and the target answers each.
The conversation is real, so a case about the fourth turn is asked at the fourth turn.

`Fixture(String name, Map<String, String> parameters)` +
The target puts itself into a named state.
The target declares what it can build in `SystemUnderTest.fixtures()`.
An experiment setup naming one it cannot is refused before the campaign runs.

`FailingTool(String tool, String message, EvalSetup then)` +
A named tool is made to fail, then the wrapped eval setup runs.
Used for cases about recovery.
A target that cannot break the named tool causes the campaign to be refused.
Answering with a working tool would report the system as recovering from a failure that
never happened.

Only `Replay` proves the system reached the state under its own power.
A fixture is a seeded starting point, and the report counts it as one.

== The six run outcomes

Every attempt ends in one of six outcomes.
Which one depends on the evaluator that settled it.

`Asserted(passed, expected, actual)` +
A deterministic evaluator compared two things.
Both sides appear on the report row, so it can print `expected GenUC-16a, found GenUC-17a`.

`Measured(metricId, metricVersion, value, threshold, passed, note)` +
A heuristic evaluator computed a value against a threshold.

`Scored(Grade)` +
A judge returned a grade against a rubric.

`Inconclusive(reason)` +
The evaluator had nothing to read.
The pass rate leaves the attempt out rather than counting it either way.

`NotReached(cause, reason, evalSetup)` +
The attempt never got to the graded turn: the fixture would not build, or the system said nothing.
A fact about the harness, not a verdict on the system.

`Failed(reason)` +
The attempt itself broke.

The last three are why the outcome type is not a number.
A `double` has no way to say "this was never asked".
A report that scores absence as zero accuses the system of something it was never given the chance to do.

== From attempts to a verdict

`EvalCaseResults` gathers an eval case's attempts and answers `verdict()` with one of five
values: `PASSED`, `FAILED`, `VARIED`, `UNDECIDED`, `NO_RESULT`.

`VARIED` is the one worth repeating for.
An eval case the system meets eight times in ten still passes five attempts about a third of
the time.
One attempt cannot tell that apart from a case it always meets.

`UNDECIDED` and `NO_RESULT` are kept apart on purpose.
A judge that would not commit and an attempt that produced no evidence at all are different facts about a campaign.

== See also

* xref:testing:evaluation/evaluators.adoc[]. The evaluators that produce each outcome.
* xref:testing:evaluation/campaigns.adoc[]. How eval cases are bundled into a campaign.
* xref:testing:evaluation/reports.adoc[]. How verdicts read on the rendered report.
