= Akka Automated Operations on Azure
:page-summary: Azure-specific setup for Akka Automated Operations — the resources Akka provisions, the identity model, resource providers to register, private connectivity, and the Azure notes for bringing your own AKS cluster.
:page-when-to-use: Use this when Akka provisions an AAO region into your Azure subscription (BYOC), or when you run AAO on your own AKS cluster (BYOK8s) on Azure.
:page-related: xref:operations:technical-overview.adoc[], xref:operations:technical-overview/byok8s-requirements.adoc[], xref:operations:technical-overview/data-flows.adoc[]
:page-persona: operator-platform, operator-sre

include::ROOT:partial$include.adoc[]

This page covers running Akka Automated Operations (AAO) on Azure. For the architecture and concepts, start with the xref:operations:technical-overview.adoc[technical overview].

[NOTE]
====
Download link:../_attachments/whitepapers/aao-azure.pdf[this page as a PDF], or the link:../_attachments/whitepapers/aao-technical-overview-full.pdf[full overview].
====

// DIAGRAM: recreate the original whitepaper Azure "multi-tenant" and "VNet peering with NAT" drawings as themed SVGs. Flagged for creator review; not yet added.

== Azure resources

AAO on Azure uses these Azure services:

* Microsoft Entra ID (applications, app registrations, service principals)
* Microsoft Azure Resource Manager (subscription, resource group, roles and role assignments)
* Networking: virtual network, subnet, public IP address, NAT gateway, load balancer, private and public DNS zones
* Compute: AKS cluster, node pools (virtual machine scale sets), network security groups
* Data: Azure Database for PostgreSQL flexible server
* Storage: storage accounts and blob containers
* Identity: managed identities and federated credentials

== Identity model

An enterprise application is registered in Microsoft Entra ID to create its identity, and a service principal is created for it. A non-human privileged role with the minimum permissions is configured and used by the Federation Plane to securely bootstrap and manage the environment.

AAO uses four roles: Deployment (created by `akka-bootstrap`), and Editor, Viewer, and SRE (created by you). The exact permissions are created and managed by `akka-bootstrap` and are the source of truth; review the generated permission reference rather than a copy here. The Editor role cannot delete resources and cannot read or write federated identity credentials.

== Resource providers to register

These register automatically through `akka-bootstrap`. Verify they are enabled on the subscription:

* `Microsoft.Authorization`
* `Microsoft.ContainerService`
* `Microsoft.DBforPostgreSQL`
* `Microsoft.ManagedIdentity`
* `Microsoft.Resources`
* `Microsoft.Network`
* `Microsoft.Storage`

The subscription must also have EncryptionAtHost enabled:

[source,command line]
----
az feature register --name EncryptionAtHost --namespace Microsoft.Compute
az provider register -n Microsoft.Compute
----

== Installation

Prepare your subscription with the `akka-bootstrap` utility, then Akka provisions the region. Create a `values.hcl` with your backend configuration and Azure region details:

[source,hcl]
----
inputs = {
  backend = {
    type = "azurerm"
    config = {
      bucket = "your-terraform-state-bucket"
      prefix = "akka-bootstrap"
    }
  }
  akka_regions = {
    azure = [
      {
        environment      = "prod"       # dev | stage | prod
        akka_region_name = "az-<tenant>-<region>"
        tenant_id        = "azure-tenant-id"
        subscription_id  = "azure-subscription-id"
      }
    ]
    aws = []
    gcp = []
  }
}
----

Then run `terragrunt init`, `terragrunt plan`, and `terragrunt apply`. After a successful apply, share the generated service-principal credentials with Akka so the Federation Plane can authenticate and manage resources in your subscription.

== Private connectivity

By default, traffic between the region and your internal networks traverses the internet. To keep it private, AAO on Azure uses Virtual Network Peering with Azure Firewall and user-defined NAT gateways. The per-flow detail is on the xref:operations:technical-overview/data-flows.adoc[data flows] page.

[#byok8s]
== Bringing your own AKS cluster (BYOK8s)

If you provision the cluster yourself, follow the shared xref:operations:technical-overview/byok8s-requirements.adoc[BYOK8s requirements] and apply the Azure specifics above (resource providers, EncryptionAtHost, and the Entra ID identity model). Use Azure Database for PostgreSQL flexible server for the database.

== See also

* xref:operations:technical-overview.adoc[Technical overview]
* xref:operations:technical-overview/byok8s-requirements.adoc[BYOK8s requirements]
* xref:operations:technical-overview/data-flows.adoc[Data flows]
