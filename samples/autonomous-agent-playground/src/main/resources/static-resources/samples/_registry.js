// Per-sample descriptors registry. Populated in US1 as each sample's descriptor lands.
// Each value is a SamplePanelDescriptor (see specs/008-samples-web-ui/data-model.md).

export const samples = {};
