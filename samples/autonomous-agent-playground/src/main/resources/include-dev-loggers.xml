<?xml version="1.0" encoding="utf-8"?>
<!--
     This is a logback include file.

     Add here the loggers you would like to have for your application during development and testing.
     To define loggers for production mode use 'include-loggers.xml'

     The loggers you define inside the <included> tag will be added to the final log configuration
     when the application runs.
-->
<included>
    <!-- Sample application code -->
    <logger name="demo.helloworld" level="DEBUG"/>

    <!-- SDK autonomous agent client -->
    <logger name="akka.javasdk.impl.client.AutonomousAgentClientImpl" level="DEBUG"/>
    <logger name="akka.javasdk.impl.client.TaskClientImpl" level="DEBUG"/>

    <!-- Runtime autonomous agent workflow -->
    <logger name="kalix.runtime.agent.autonomous" level="DEBUG"/>
</included>
