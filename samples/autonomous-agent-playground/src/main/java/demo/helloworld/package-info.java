/**
 * Hello World — single autonomous agent, no tools, no coordination.
 *
 * <p>The simplest possible autonomous agent sample. A single agent answers questions using only its
 * LLM capabilities — no domain tools, no coordination with other agents.
 *
 * <p><b>Capabilities demonstrated:</b> Basic autonomous agent lifecycle (create task, assign to
 * agent, poll for result).
 *
 * <p><b>See:</b> {@code http/helloworld.http} for example requests.
 */
package demo.helloworld;
