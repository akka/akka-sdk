include "application.conf"

my-app {
  some-feature-flag = false
}

# application.conf declares backoffice services for the snippets. Resolving them needs the Akka
# platform, which a test does not have.
akka.javasdk.dev-mode.backoffice.enabled = false
